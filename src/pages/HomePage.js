const HomePage = (props) => {
  return (
    <section className="pagelayout">
      <h1 className="maintitle">Welcome!</h1>
      <p> this is a sample page to implement single page applications using react router v6,</p>
    </section>
  );
};

export default HomePage;
